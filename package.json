{
  "name": "crosslink",
  "version": "1.0.0",
  "description": "CrossLink - Cross-sector collaboration platform connecting NGOs, Volunteers, and Corporates",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["crosslink", "ngo", "volunteer", "corporate", "collaboration", "social-impact"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
